export type CrmContact = {
  id: string;
  firstName: string;
  lastName: string;
  emailAddress: string;
};

export type LibraryPerson = {
  name: string;
  title: string;
  location: string;
  email: string;
};
